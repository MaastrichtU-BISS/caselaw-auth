# Keep the complete, upgrade-resilient PatternFly coverage from caselaw and
# apply DigiMach's tokens after it. Account themes are selected per realm, not
# per OIDC client; see ../README.md before enabling this on a shared realm.
parent=caselaw
styles=css/caselaw-account.css css/digimach-account-v1.css

footerManagedBy=${env.DIGIMACH_AUTH_FOOTER_MANAGED_BY:Authentication managed by BISS Institute}
