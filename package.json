{
  "name": "caselaw-auth-workspace",
  "private": true,
  "workspaces": [
    "packages/caselaw-auth"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node --test test/ && npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces"
  }
}
